/** @type {import('next').NextConfig} */
const nextConfig = {
     basePath: "/kape",
     output: "export",
     images: { unoptimized: true },
     reactStrictMode: true
};

export default nextConfig;
